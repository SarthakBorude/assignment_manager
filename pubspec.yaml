name: assignment_manager
description: A new Flutter project.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Make sure you are on Flutter 3.10+

dependencies:
  flutter:
    sdk: flutter
  
  # For State Management
  flutter_riverpod: ^2.5.1
  
  # For Supabase (you'll add these for the real app)
  # supabase_flutter: ^2.0.0
  
  # For file picking (you'll add this)
  # file_picker: ^8.0.0
  
  # For opening/sharing files (you'll add this)
  # open_file: ^3.3.2
  # share_plus: ^9.0.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

